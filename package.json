{
  "name": "agent-audit-trail-workspace",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "build": "pnpm -r run build",
    "test": "vitest run",
    "test:coverage": "vitest run --coverage",
    "typecheck": "pnpm -r run typecheck",
    "lint": "oxlint .",
    "format": "oxfmt --check .",
    "format:fix": "oxfmt --write ."
  },
  "devDependencies": {
    "@vitest/coverage-v8": "^2.1.0",
    "commander": "^12.1.0",
    "typescript": "^5.7.0",
    "vitest": "^2.1.0"
  }
}
